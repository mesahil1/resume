[build]
  command   = "node build.js"
  publish   = "dist"

# Env vars (SUPABASE_URL, SUPABASE_ANON_KEY) are set in the
# Netlify dashboard → Site Settings → Environment Variables.
# They are injected at build time by build.js — never stored in code.
